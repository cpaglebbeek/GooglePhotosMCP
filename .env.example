# Google Photos MCP — Environment Variables
# Copy this to .env and adjust as needed

# Optional: custom config directory (default: ~/.config/google-photos-mcp/)
# GOOGLE_PHOTOS_CONFIG_DIR=/path/to/config
